name: Test on all the OS/Platforms

on: [pull_request]

jobs:
  lua:
    strategy:
      fail-fast: false
      matrix:
        os: [linux, macos, macos-arm64]
        lua: [lua=5.1, lua=5.4, luajit=@v2.1]
        include:
          - os: linux
            runner: ubuntu-latest
          - os: macos
            runner: macos-15-intel
          - os: macos-arm64
            runner: macos-latest

    name: ${{ matrix.os }} (${{ matrix.lua }})
    runs-on: ${{ matrix.runner }}
    steps:
      # Checks-out the repository under $GITHUB_WORKSPACE.
      - uses: actions/checkout@v6
      - name: Install libreadline
        if: runner.os == 'Linux'
        run: |
          sudo apt-get install -y libreadline-dev

      - name: Cache Restore
        uses: actions/cache/restore@v5
        with:
          path: |
            lua_install
          key: ${{ matrix.lua }}-${{ matrix.os }}-lua_install

      - name: Install Lua (${{ matrix.lua }})
        run: |
          pip install git+https://github.com/luarocks/hererocks
          hererocks lua_install -r^ --${{ matrix.lua }}
          echo lua_install/bin >> $GITHUB_PATH
        env:
          MACOSX_DEPLOYMENT_TARGET: 11.0

      - name: Build ts-teal
        run: |
          luarocks make
          luarocks install ltreesitter

      - name: Run tests
        run: |
          lua test/quick_test.lua

      - name: Save Cache
        uses: actions/cache/save@v5
        with:
          path: |
            lua_install
          key: ${{ matrix.lua }}-${{ matrix.os }}-lua_install


  windows:
    strategy:
      fail-fast: false
      matrix:
        lua: [lua=5.1, lua=5.4, luajit=@v2.1]
        target: [mingw, vs]
    runs-on: windows-2022
    steps:
      # Checks-out the repository under $GITHUB_WORKSPACE.
      - uses: actions/checkout@v6

      - name: Cache Restore
        uses: actions/cache/restore@v5
        with:
          path: |
            lua_install
          key: ${{ matrix.lua }}-${{ matrix.target }}-lua_install

      - name: Install Lua (${{ matrix.lua }})
        run: |
          pip install git+https://github.com/luarocks/hererocks
          hererocks lua_install -r^ --${{ matrix.lua }} --target ${{ matrix.target }}

      - name: Build ts-teal
        run: |
          .\lua_install\bin\activate
          luarocks make
          luarocks install ltreesitter

      - name: Run tests
        run: |
          .\lua_install\bin\activate
          lua test/quick_test.lua

      - name: Save Cache
        uses: actions/cache/save@v5
        with:
          path: |
            lua_install
          key: ${{ matrix.lua }}-${{ matrix.target }}-lua_install
